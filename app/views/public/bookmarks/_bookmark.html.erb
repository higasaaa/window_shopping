
<% if coordinate.bookmarked_by?(current_customer) %>
  <%= link_to "お気に入りから外す", coordinate_bookmarks_path(coordinate), method: :delete %>
<% else %>
  <%= link_to "お気に入り登録する", coordinate_bookmarks_path(coordinate), method: :post %>
<% end %>
